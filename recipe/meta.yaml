{% set name = "b2sdk" %}
{% set version = "2.13.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/b2sdk-{{ version }}.tar.gz
  sha256: edb586be7698b4b868194506cd81d594364e445817461c7e7b43df47378f6827

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - hatchling >=1.24.2
    - hatch-vcs >=0.4
    - pip
  run:
    - python >={{ python_min }}
    - annotated-types >=0.5.0
    - logfury <2.0.0,>=1.0.1
    - requests <3.0.0,>=2.33.0
    - typing-extensions >=4.7.1

test:
  imports:
    - b2sdk
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/Backblaze/b2-sdk-python
  summary: Backblaze B2 SDK
  license: MIT AND Apache-2.0
  license_file:
    - LICENSE
    - b2sdk/_internal/requests/LICENSE
    - b2sdk/_internal/requests/NOTICE

extra:
  recipe-maintainers:
    - freundTech
